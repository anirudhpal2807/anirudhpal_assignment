// Department enum
export enum Department {
  CSE = 'CSE',
  ECE = 'ECE',
  ME = 'ME',
  CE = 'CE',
  EE = 'EE'
}

// Year enum
export enum Year {
  FIRST = 1,
  SECOND = 2,
  THIRD = 3,
  FOURTH = 4
}

// Sort options enum
export enum SortOption {
  NAME_ASC = 'name_asc',
  NAME_DESC = 'name_desc',
  CGPA_ASC = 'cgpa_asc',
  CGPA_DESC = 'cgpa_desc'
}

// Sort direction enum
export enum SortDirection {
  ASC = 'asc',
  DESC = 'desc'
}